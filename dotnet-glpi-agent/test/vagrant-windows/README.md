# Windows Vagrant baseline

This directory is reserved for the dedicated Windows Server 2022 validation VM.
The test workflow must destroy only the VM defined here after artifacts have
been collected.
